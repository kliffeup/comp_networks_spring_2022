Задание 1 (Wireshark, IP).
1. IP-адрес моего компьютера: 192.168.1.107.
2. Значение протокола: 1, следуя расшифровке Wireshark, этот протокол -  ICMP.
3. На заголовок приходится 20 байт, на полезную нагрузку приходится total length - header length = 36 байт.
4. 
a + b. Не меняются:
        * Version;
        * Header length;
        * Diff. Services Field;
        * Total length (мы задали его в программе pingplotter.com);
        * Flags;
        * Protocol;
        * Header Checksum (она почему-то отключена, всегда остаётся 0);
        * Source Address;
        * Destination Address;
       Изменяются:
        * Identification;
        * TTL;
       Должны не изменяться (в рамках отсылки от нашего компьютера):
        * Version;
        * Source Address;
       Должны изменяться :
        * ("Не меняются" \setminus "Должны не изменяться") \setunion "Изменяются".
c. Да, закономерность имеется: каждый последующий эхо-запрос имеет Identification на 1 больше, чем предыдущий.
   То есть просто инкрементируется на 1.
5. Значение Identification для первого посланного эхо-запроса: 0x2225 (8741 в десятичной).
   Значение Time to Live для первого посланного эхо-запроса: 255.
6. Нет, эти значения меняются.
   На примере 25 пакета (ответ на 24 пакет, второй отправленный эхо-запрос), отправленный с 192.168.1.1
   (судя по pingplotter.com, это ближайший маршрутизатор на пути).
   25 пакет содержит информацию о time-to-live-exceeded 24 пакета.

   Под теми же значениями я здесь имею в виду обозначеные в задании 5 поля Identification и Time to Live.

   24 пакет:
    Identification: 0x2226.
    Time to Live: 1.

   25 пакет:
    Identification: 0x03db.
    Time to Live: 64.
7. На примере из предыдущего задания, где 25 пакет показывает time-to-live-exceeded 24 пакета.
   25 пакет:
    Identification: 0x03db.
    Time to Live: 64.
8.
a. Да, оно было фрагментировано на 3 фрагмента: первые два (7 и 8 пакеты) по 1480 байт, последний (9) - 520 байт.
b. Изменяются:
    * Total length: 1500 у первых двух, 540 - у последнего.
    * Flags: 0x20, More fragments у первых двух, 0x01 - у последнего.
    * Fragment Offset у всех трёх разный: 0 у первого, 1480 у второго, 2960 у последнего.
    * Header Checksum у всех трёх разный: у первого - 0xd2bb, у второго - 0xd202, у последнего - 0xf509.
